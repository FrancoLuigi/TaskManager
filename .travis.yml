matrix:
   include:
  
  - language: java
   before_script:
      - cd TaskManager
      - chmod +x gradlew
   script:
      - ./gradlew clean

   
   - language: android
      android:
      components:
         - build-tools-28.0.3
         - android-28
         - android-24
         - sys-img-armeabi-v7a-android-24
         - tools
         - platform-tools 
     before_script:
      - echo no | android create avd --force -n test -t android-24 --abi armeabi-v7a
      - echo y | android update sdk --no-ui --filter build-tools-28.0.3,android-28,extra-android-m2repository
      - emulator -avd test -no-window &
      - android-wait-for-emulator
      - adb shell input keyevent 82 &
      
    script:
      - cd ..
      - cd TaskManagerAndroidClient
      - chmod +x gradlew
      - ./gradlew connectedAndroidTest

sudo: true

jdk:
- oraclejdk8

services:
- mysql

before_install:
- echo mysql-apt-config mysql-apt-config/select-server select mysql-5.7 | sudo debconf-set-selections
- wget https://dev.mysql.com/get/mysql-apt-config_0.7.3-1_all.deb
- sudo dpkg --install mysql-apt-config_0.7.3-1_all.deb
- sudo apt-get update -q
- sudo apt-get install -q -y --allow-unauthenticated -o Dpkg::Options::=--force-confnew mysql-server
- sudo mysql_upgrade
- sudo apt-get update -y
- sudo service mysql restart

notifications:
email:
- francoluigi95@gmail.com
- davidecocca@gmail.com
- giovannipirozzi95@gmail.com
on_success: never
on_failure: always

notifications:
  webhooks: https://fathomless-fjord-24024.herokuapp.com/notify
  


sudo: required
dist: trusty
group: stable
